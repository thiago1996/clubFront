import { FiltrarCategoriaPipe } from './filtrar-categoria.pipe';

describe('FiltrarCategoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarCategoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
