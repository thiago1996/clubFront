import { FiltrarPagoServicioPipe } from './filtrar-pagoServicio.pipe';

describe('FiltrarPagoServicioPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarPagoServicioPipe();
    expect(pipe).toBeTruthy();
  });
});
