import { FiltrarEntrenadorPipe } from './filtrar-entrenador.pipe';

describe('FiltrarEntrenadorPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarEntrenadorPipe();
    expect(pipe).toBeTruthy();
  });
});
