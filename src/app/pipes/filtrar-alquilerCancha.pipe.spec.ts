import { FiltrarAlquilerCanchaPipe } from './filtrar-alquilerCancha.pipe';

describe('FiltrarAlquilerCanchaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarAlquilerCanchaPipe();
    expect(pipe).toBeTruthy();
  });
});
