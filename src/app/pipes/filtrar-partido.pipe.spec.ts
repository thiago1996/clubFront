import { FiltrarPartidoPipe } from './filtrar-partido.pipe';

describe('FiltrarPartidoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarPartidoPipe();
    expect(pipe).toBeTruthy();
  });
});
