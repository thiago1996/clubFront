import { FiltrarPersonaPipe } from './filtrar-persona.pipe';

describe('FiltrarPersonaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarPersonaPipe();
    expect(pipe).toBeTruthy();
  });
});
