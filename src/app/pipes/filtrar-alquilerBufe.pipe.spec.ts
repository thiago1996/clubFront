import { FiltrarAlquilerBufePipe } from './filtrar-alquilerbufe.pipe';

describe('FiltrarAlquilerBufePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarAlquilerBufePipe();
    expect(pipe).toBeTruthy();
  });
});
